{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**", ".solid/**", ".vercel/**"]
    },
    "dev": {
      "cache": false
    },
    "clean": { "cache": false }
  }
}
